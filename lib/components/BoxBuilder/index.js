import { Box } from '@material-ui/core';
import styled from 'styled-components';
export const BoxBuilder = styled(Box) `
  .query-builder {
    margin: 0;
  }

  .group {
    background: rgba(2, 99, 165, 0.1);
    border: 1px solid #0263a5;
  }

  .query-builder-container {
    padding: 0 !important;
  }

  .group--actions,
  .rule--drag-handler,
  .rule--header {
    opacity: 1 !important;
  }
`;
//# sourceMappingURL=index.js.map